# Ejemplos de Variables de Entorno
# Copiar a local.settings.json (para desarrollo local)
# O configurar en Azure Portal → Configuration (para producción)

{
  "IsEncrypted": false,
  "Values": {
    # Azure Functions Runtime
    "AzureWebJobsStorage": "UseDevelopmentStorage=true",
    "FUNCTIONS_WORKER_RUNTIME": "node",
    
    # Base de datos - Azure Table Storage
    "STORAGE_CONNECTION_STRING": "<TU_CONNECTION_STRING_AQUI>",
    
    # JWT Secret - ⚠️ CAMBIAR EN PRODUCCIÓN
    # Generar con: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
    "JWT_SECRET": "cambiar-este-secret-por-uno-aleatorio-largo-en-produccion",
    
    # Opcional: ELIT API (para importación de productos)
    "ELIT_USER_ID": "",
    "ELIT_TOKEN": "",
    
    # Opcional: NewBytes API
    "NEWBYTES_API_KEY": ""
  }
}

# NOTAS:
# 1. Este archivo NO debe subirse a Git (ya está en .gitignore)
# 2. En Azure, configurar estas variables en Static Web App → Configuration
# 3. JWT_SECRET debe ser diferente en dev y producción
# 4. STORAGE_CONNECTION_STRING lo obtienes de Azure Portal → Storage Account → Access Keys
